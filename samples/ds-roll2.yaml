apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: ds-roll2
spec:
  selector:
    matchLabels:
      system: DaemonSetRoll
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        system: DaemonSetRoll
    spec:
      containers:
      - name: nginx
        image: nginx:1.19.3
        ports:
        - containerPort: 80
